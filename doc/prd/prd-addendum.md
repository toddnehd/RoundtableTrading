# PRD 보완 사항 (Addendum)
# RoundtableTrading - 타임프레임 전략 & 로컬 LLM

**버전**: v0.2  
**작성일**: 2026-01-27  
**관련 문서**: prd.md v0.2

---

## 1. 타임프레임별 전략 시스템 (Phase 1 포함)

### 1.1 개요

**문제 인식**:
- 현재 PRD의 에이전트는 분야별(펀더멘털/기술적/시장/리스크)로만 분류
- **시간축(타임프레임)에 대한 고려 부족**
- 같은 종목이라도 "1주일 스윙"과 "3개월 포지션"은 완전히 다른 판단 필요

**해결 방안**:
- 타임프레임별 전략 시스템 도입
- Phase 1에서는 **단기/중기/장기** 3가지 타임프레임 지원 (초단기 제외)

---

### 1.2 타임프레임 정의

| 타임프레임 | 보유 기간 | Phase 1 포함 | 특징 |
|-----------|----------|-------------|------|
| **초단기 (데이트레이딩)** | 1일 이내 | ❌ 제외 | 장중 실시간 대응 필요, Phase 2 이후 |
| **단기 (스윙)** | 3~10일 | ✅ 포함 | 기술적 분석 중심, 단기 모멘텀 |
| **중기 (포지션)** | 1~3개월 | ✅ 포함 | 기술적 + 펀더멘털 균형 |
| **장기 (투자)** | 3개월 이상 | ✅ 포함 | 펀더멘털 중심, 기업 가치 |

**Phase 1 제외 이유 (초단기)**:
- 장중 실시간 모니터링 필요 (Phase 1은 장 후 분석 중심)
- LLM 호출 레이턴시(1-5초)로 인해 빠른 대응 어려움
- Phase 2에서 규칙 기반 시스템과 결합하여 구현 예정

---

### 1.3 타임프레임별 에이전트 가중치

**핵심 아이디어**:
- 타임프레임에 따라 에이전트의 중요도가 다름
- 조정자가 타임프레임을 먼저 결정하고, 해당 타임프레임에 맞는 가중치 적용

**가중치 매트릭스**:

| 에이전트 | 단기 (3~10일) | 중기 (1~3개월) | 장기 (3개월+) |
|---------|--------------|---------------|--------------|
| **기업 가치 분석** | 0.7 | 1.0 | 1.5 |
| **기술적 분석** | 1.5 | 1.2 | 0.8 |
| **시장 분석** | 1.0 | 1.0 | 1.0 |
| **리스크 관리** | 1.0 | 1.0 | 1.0 |
| **조정자** | 1.0 | 1.0 | 1.0 |

**해석**:
- **단기**: 기술적 분석 가중치 ↑ (1.5), 펀더멘털 ↓ (0.7)
- **장기**: 펀더멘털 가중치 ↑ (1.5), 기술적 분석 ↓ (0.8)
- **중기**: 균형잡힌 가중치

---

### 1.4 구현 방식

#### 1.4.1 사용자 인터페이스

**Streamlit 대시보드에 타임프레임 선택 추가**:

```python
# Streamlit UI
timeframe = st.selectbox(
    "투자 기간 선택",
    options=["단기 (3~10일)", "중기 (1~3개월)", "장기 (3개월 이상)"],
    index=1  # 기본값: 중기
)

ticker = st.text_input("종목 코드 또는 이름", "삼성전자")

if st.button("분석 시작"):
    result = analyze_stock(ticker, timeframe)
    display_result(result)
```

#### 1.4.2 조정자 로직 수정

**기존**:
```python
class Moderator:
    def make_decision(self, opinions: list[AgentOpinion]):
        # 모든 에이전트 의견을 동일 가중치로 처리
        weighted_score = sum(op.score * op.agent.weight for op in opinions)
        ...
```

**개선**:
```python
class Moderator:
    TIMEFRAME_WEIGHTS = {
        "단기": {"fundamental": 0.7, "technical": 1.5, "market": 1.0, "risk": 1.0},
        "중기": {"fundamental": 1.0, "technical": 1.2, "market": 1.0, "risk": 1.0},
        "장기": {"fundamental": 1.5, "technical": 0.8, "market": 1.0, "risk": 1.0},
    }
    
    def make_decision(self, opinions: list[AgentOpinion], timeframe: str):
        # 타임프레임별 가중치 적용
        tf_weights = self.TIMEFRAME_WEIGHTS[timeframe]
        
        weighted_score = sum(
            op.score * op.agent.weight * tf_weights[op.agent.type]
            for op in opinions
        )
        ...
```

#### 1.4.3 리포트 출력 예시

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
삼성전자 (005930) 분석 결과
투자 기간: 중기 (1~3개월)
신뢰도: 82% | 최종 결정: 매수 추천
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 찬성 (4명)
├─ 기업가치: 긍정 (85점) [가중치: 1.0]
│  └─ PER 15 역사적 저평가, 실적 개선
├─ 기술적: 긍정 (80점) [가중치: 1.2] ⬆️
│  └─ 골든크로스, 거래량 증가
├─ 시장: 긍정 (78점) [가중치: 1.0]
│  └─ 반도체 업황 회복 신호
└─ 리스크: 중립 (65점) [가중치: 1.0]
   └─ 변동성 낮음, 유동성 우수

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
조정자 최종 판단 (중기 전략)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
매수 추천: 70,000원
목표가: 78,000원 (+11.4%)
손절가: 66,000원 (-5.7%)
포지션: 총 자산의 15%
예상 보유 기간: 1~3개월

💡 중기 전략 포인트:
- 기술적 지표와 펀더멘털 모두 긍정적
- 1~2주 내 단기 조정 가능성 있으나 중기 상승 전망
- 분할 매수 권장 (3회)
```

---

### 1.5 백테스팅 전략

**타임프레임별 백테스팅**:
- 각 타임프레임별로 별도 백테스팅 수행
- 성과 지표를 타임프레임별로 분리 추적

```python
# 백테스팅 실행
results = {
    "단기": backtest(strategy="short_term", holding_period="3-10일"),
    "중기": backtest(strategy="medium_term", holding_period="1-3개월"),
    "장기": backtest(strategy="long_term", holding_period="3개월+"),
}

# 결과 비교
for timeframe, result in results.items():
    print(f"{timeframe}: 샤프 비율 {result.sharpe}, 승률 {result.win_rate}")
```

**예상 결과**:
- 단기: 승률 높지만 거래 비용 많음
- 중기: 균형잡힌 성과
- 장기: 샤프 비율 높지만 거래 횟수 적음

---

### 1.6 Phase 1 구현 우선순위

| 항목 | 우선순위 | 비고 |
|------|---------|------|
| 타임프레임 선택 UI | P0 (필수) | Streamlit selectbox |
| 가중치 매트릭스 구현 | P0 (필수) | Moderator 클래스 수정 |
| 타임프레임별 리포트 | P1 (중요) | 가중치 표시 추가 |
| 타임프레임별 백테스팅 | P1 (중요) | 성과 비교 |
| 복수 타임프레임 동시 분석 | P2 (선택) | Phase 2 이후 |

---

## 2. 로컬 LLM 활용 (비용 절감)

### 2.1 개요

**문제 인식**:
- Claude API 비용이 Phase 1에서도 월 5-10만원 예상
- Phase 3에서 사용자 증가 시 비용 폭증 가능성
- 일부 단순 작업은 고성능 LLM 불필요

**해결 방안**:
- **하이브리드 LLM 전략**: 클라우드 LLM + 로컬 LLM 병행
- 복잡도에 따라 적절한 모델 선택

---

### 2.2 로컬 LLM 후보

| 모델 | 크기 | 성능 | 추론 속도 | 추천도 |
|------|------|------|----------|--------|
| **Llama 3.1 8B** | 8B | 중상 | 빠름 | ⭐⭐⭐⭐⭐ |
| **Mistral 7B** | 7B | 중 | 빠름 | ⭐⭐⭐⭐ |
| **Phi-3 Mini** | 3.8B | 중하 | 매우 빠름 | ⭐⭐⭐ |
| **Qwen 2.5 7B** | 7B | 중상 | 빠름 | ⭐⭐⭐⭐ |

**권장**: Llama 3.1 8B
- 성능과 속도의 균형
- 한국어 지원 양호
- Ollama로 쉽게 설치 가능

---

### 2.3 하이브리드 LLM 전략

#### 2.3.1 작업별 LLM 할당

| 작업 | 복잡도 | 사용 모델 | 이유 |
|------|--------|----------|------|
| **기술적 지표 계산** | 낮음 | 규칙 기반 (Python) | 수식 계산, LLM 불필요 |
| **기술적 지표 해석** | 중 | 로컬 LLM (Llama 3.1 8B) | 단순 패턴 인식 |
| **뉴스 감성 분석** | 중 | 로컬 LLM (Llama 3.1 8B) | 긍정/부정 분류 |
| **재무제표 분석** | 높음 | Claude 3.5 Sonnet | 복잡한 추론 필요 |
| **시장 분석** | 높음 | Claude 3.5 Sonnet | 거시 경제 이해 |
| **리스크 평가** | 높음 | Claude 3.5 Sonnet | 고도의 판단 |
| **조정자 (최종 결정)** | 높음 | Claude 3.5 Sonnet | 종합 판단 |

#### 2.3.2 비용 절감 효과

**기존 (모두 Claude Sonnet)**:
```
일 10종목 × 5 에이전트 × 평균 3K tokens = 150K tokens/일
월 비용: 150K × 20일 × ($3 input + $15 output) / 1M ≈ $54 (약 7만원)
```

**개선 (하이브리드)**:
```
로컬 LLM 처리 (기술적 해석, 뉴스 감성): 40% → 비용 $0
Claude Sonnet (펀더멘털, 시장, 리스크, 조정자): 60%

월 비용: $54 × 0.6 = $32.4 (약 4.2만원)
절감: 40%
```

**추가 절감 (프롬프트 캐싱)**:
- 캐싱 적용 시 90% 절감 (캐싱 부분)
- **최종 예상 비용: 월 2-3만원** ✅

---

### 2.4 구현 방식

#### 2.4.1 Ollama 설치 및 설정

```bash
# Ollama 설치 (macOS)
brew install ollama

# Llama 3.1 8B 다운로드
ollama pull llama3.1:8b

# 서버 실행
ollama serve
```

#### 2.4.2 LLM 클라이언트 추상화

```python
from abc import ABC, abstractmethod

class LLMClient(ABC):
    @abstractmethod
    async def generate(self, prompt: str, **kwargs) -> str:
        pass

class ClaudeClient(LLMClient):
    def __init__(self, api_key: str):
        self.client = anthropic.Anthropic(api_key=api_key)
    
    async def generate(self, prompt: str, **kwargs) -> str:
        response = await self.client.messages.create(
            model="claude-3-5-sonnet-20241022",
            messages=[{"role": "user", "content": prompt}],
            **kwargs
        )
        return response.content[0].text

class OllamaClient(LLMClient):
    def __init__(self, model: str = "llama3.1:8b"):
        self.model = model
        self.base_url = "http://localhost:11434"
    
    async def generate(self, prompt: str, **kwargs) -> str:
        async with httpx.AsyncClient() as client:
            response = await client.post(
                f"{self.base_url}/api/generate",
                json={"model": self.model, "prompt": prompt, **kwargs}
            )
            return response.json()["response"]
```

#### 2.4.3 에이전트별 LLM 할당

```python
class AgentFactory:
    def __init__(self):
        self.claude = ClaudeClient(api_key=os.getenv("ANTHROPIC_API_KEY"))
        self.ollama = OllamaClient(model="llama3.1:8b")
    
    def create_agent(self, agent_type: str) -> BaseAgent:
        llm_mapping = {
            "fundamental": self.claude,  # 복잡한 재무 분석
            "technical": self.ollama,    # 단순 패턴 인식
            "market": self.claude,       # 거시 경제 추론
            "risk": self.claude,         # 고도의 판단
            "moderator": self.claude,    # 최종 의사결정
        }
        
        llm = llm_mapping[agent_type]
        return BaseAgent(name=agent_type, llm_client=llm)
```

#### 2.4.4 성능 비교 (로컬 vs 클라우드)

```python
# 벤치마크 테스트
async def benchmark_llm():
    test_prompt = "다음 기술적 지표를 해석하세요: RSI 65, MACD 골든크로스"
    
    # Claude
    start = time.time()
    claude_result = await claude_client.generate(test_prompt)
    claude_time = time.time() - start
    
    # Ollama
    start = time.time()
    ollama_result = await ollama_client.generate(test_prompt)
    ollama_time = time.time() - start
    
    print(f"Claude: {claude_time:.2f}초, 비용: $0.001")
    print(f"Ollama: {ollama_time:.2f}초, 비용: $0")
```

**예상 결과**:
- Claude: 1-2초, 고품질 응답
- Ollama: 2-5초 (M1/M2 Mac 기준), 중간 품질 응답

---

### 2.5 로컬 LLM 품질 검증

**Phase 1 초기 전략**:
1. **Week 1-4**: 모든 에이전트 Claude 사용 (기준선 확립)
2. **Week 5**: 기술적 분석 에이전트만 Ollama로 전환
3. **Week 6**: 성과 비교 (Claude vs Ollama)
   - 백테스팅 샤프 비율 비교
   - 승률 비교
   - 응답 품질 평가
4. **Week 7-8**: 품질 차이 < 5% 시 Ollama 유지, 아니면 Claude 복귀

**품질 평가 기준**:
```python
def evaluate_agent_quality(agent_name: str, llm_type: str):
    # 과거 100개 종목 분석
    predictions = []
    for ticker in test_tickers:
        prediction = agent.analyze(ticker)
        predictions.append(prediction)
    
    # 2주 후 실제 결과와 비교
    accuracy = calculate_accuracy(predictions, actual_results)
    
    return {
        "agent": agent_name,
        "llm": llm_type,
        "accuracy": accuracy,
        "avg_confidence": np.mean([p.score for p in predictions])
    }
```

---

### 2.6 하드웨어 요구사항

**로컬 LLM 실행 최소 사양**:
- CPU: Apple M1 이상 또는 Intel i7 이상
- RAM: 16GB 이상 (8B 모델 기준)
- 저장공간: 10GB (모델 파일)

**추론 속도**:
- M1 Mac: Llama 3.1 8B → 약 20-30 tokens/sec
- M2 Mac: Llama 3.1 8B → 약 30-40 tokens/sec
- Intel Mac: 느림 (권장하지 않음, Claude 사용)

**대안 (하드웨어 부족 시)**:
- 모든 에이전트 Claude 사용
- 2단계 스크리닝 + 캐싱으로 비용 절감
- 예상 비용: 월 5-7만원 (여전히 목표 범위 내)

---

### 2.7 Phase 1 구현 우선순위

| 항목 | 우선순위 | 비고 |
|------|---------|------|
| LLM 클라이언트 추상화 | P1 (중요) | 향후 확장성 |
| Ollama 설치 및 테스트 | P1 (중요) | Week 5 시작 전 |
| 기술적 분석 에이전트 Ollama 전환 | P2 (선택) | 품질 검증 후 결정 |
| 성과 비교 시스템 | P2 (선택) | Claude vs Ollama |
| 뉴스 감성 분석 Ollama 전환 | P3 (미래) | Phase 2 이후 |

---

## 3. PRD 업데이트 요약

### 3.1 추가된 기능 요구사항

**F9. 타임프레임별 전략 시스템** (P0 - 필수)
- 단기/중기/장기 3가지 타임프레임 지원
- 타임프레임별 에이전트 가중치 자동 조정
- 사용자 인터페이스에 타임프레임 선택 추가
- 타임프레임별 백테스팅 및 성과 비교

**F10. 하이브리드 LLM 시스템** (P1 - 중요)
- LLM 클라이언트 추상화 (Claude, Ollama)
- 작업 복잡도에 따른 LLM 자동 선택
- 로컬 LLM (Llama 3.1 8B) 통합
- 성과 비교 및 품질 검증 시스템

### 3.2 비용 구조 업데이트

**기존 예상 (PRD v0.1)**:
- 월 LLM 비용: 5-10만원

**개선 예상 (PRD v0.2)**:
- 하이브리드 LLM 적용 시: 월 2-3만원 (40% 절감)
- 로컬 LLM 미사용 시: 월 5-7만원 (여전히 목표 범위 내)

### 3.3 기술 스택 업데이트

**추가**:
- **Ollama**: 로컬 LLM 실행 환경
- **Llama 3.1 8B**: 중간 복잡도 작업용 로컬 모델

### 3.4 로드맵 업데이트

**Week 5-6 추가 작업**:
- [ ] Ollama 설치 및 Llama 3.1 8B 다운로드
- [ ] LLM 클라이언트 추상화 구현
- [ ] 기술적 분석 에이전트 Ollama 전환 (선택)
- [ ] 타임프레임 선택 UI 구현
- [ ] 타임프레임별 가중치 시스템 구현

---

## 4. 다음 단계

### 4.1 즉시 결정 필요

1. **로컬 LLM 사용 여부**:
   - [ ] 사용 (하드웨어 충분, 비용 절감 우선)
   - [ ] 미사용 (하드웨어 부족, 품질 우선)
   - [ ] Week 5에 결정 (일단 Claude로 시작)

2. **타임프레임 기본값**:
   - [ ] 단기 (3~10일)
   - [x] 중기 (1~3개월) - 권장
   - [ ] 장기 (3개월+)

### 4.2 검증 필요 사항

1. **로컬 LLM 성능 테스트**:
   - Ollama 설치 후 Llama 3.1 8B 추론 속도 측정
   - 기술적 지표 해석 품질 평가
   - Claude와 비교

2. **타임프레임별 백테스팅**:
   - 각 타임프레임별 성과 차이 확인
   - 최적 타임프레임 도출

---

**문서 버전**: v0.2  
**최종 업데이트**: 2026-01-27  
**다음 리뷰**: Week 5 (로컬 LLM 품질 검증 후)
